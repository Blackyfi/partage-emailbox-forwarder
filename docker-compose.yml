version: '3.8'

services:
  forwarder:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
